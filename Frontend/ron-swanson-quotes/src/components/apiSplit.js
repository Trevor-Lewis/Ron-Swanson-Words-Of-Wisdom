import React from 'react';

const apiSplit = () => {
    let small = []
    let medium = []
    let large = []

    this.END_POINT = 'https://ron-swanson-quotes.herokuapp.com/v2/quotes/58'

    // filter through the array
    const small_quote = this.END_POINT.forEach( function (small) {
        if (QuoteCounter(quote) <= 4) {
            small.push()
            return small;
        }
    })
    // sort sizes if the elements into small, medium, or large
    // return the new arrays
}

export default apiSplit;